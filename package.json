{
  "name": "salati",
  "version": "1.0.18",
  "description": "Horarios de oración, brújula de la Qibla, mezquitas cercanas, calendario hijri y Corán.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-www.mjs",
    "native:apply": "node scripts/apply-native.mjs",
    "android:prepare": "npm run build && npx cap add android && npm run native:apply && npx cap sync android",
    "android:sync": "npm run build && npm run native:apply && npx cap sync android"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app-launcher": "^8.0.0",
    "@capacitor/browser": "^8.0.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/geolocation": "^8.0.0",
    "@capacitor/local-notifications": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
